if-shell '[ -f /usr/share/tmux/powerline.conf ]' 'source-file /usr/share/tmux/powerline.conf'

unbind C-b
set -g prefix M-a
set -g base-index 1
set -s escape-time 0
bind-key a send-prefix
bind-key b set-window-option synchronize-panes

