#!/bin/sh

cmd=$1
shift;

case $cmd in
    unmute)
        pactl set-source-mute 0 0
        notify 1 "Mic Unmuted"
        ;;
    mute)
        pactl set-source-mute 0 1
        notify 1 "Mic Muted"
        ;;
    *)
        echo "No Such command"
        ;;
esac
