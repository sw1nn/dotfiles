#!/bin/sh

cmd=$1
shift;

case $cmd in
    up)
        systemctl start openvpn@home-access.service
        notify 1 "VPN Start"
        ;;
    down)
        systemctl stop openvpn@home-access.service
        notify 1 "VPN Stop"
        ;;
    *)
        echo "No Such command"
        ;;
esac
